# アプリ仕様（index.html）

※「今の正」だけを書く。経緯・改修履歴はgit log。仕様を変えたらこのdocも更新すること。

## データモデルと計算
- Firebase `sessions/{10文字ID}`（URL共有モデル）＋ localStorage `mahjong-v3`（履歴最大`MAX_HISTORY`**100件**・登録名）
- **履歴が消えにくくなる対策（第1段階・2026-08）**:
  ①`saveStorage`は容量あふれ（QuotaExceeded）で丸ごと失敗せず、**古い履歴から落として必ず保存を成功させる**
  （gamesは常に新しい順＝末尾が最古）
  ②履歴を持つ端末では`requestPersistentStorage()`＝`navigator.storage.persist()`で保存データの保護を要求
  （起動時とsaveToLocalHistory。Chrome系/Firefoxが利用実績から自動判断・ダイアログなし。Safariは非対応）
  ③過去の試合画面に**「ホーム画面に追加」の案内**（`showInstallHelp`モーダル。**iPhone→Android→PCの順で固定**。
  iPhoneはLP番外編と同テイストのSVGイラスト3枚で図解: ①アドレスバー右の「…」→②「共有」→③「ホーム画面に追加」→④「追加」。
  実機スクショ（まじゃすこ素材/iPhone画面/）を簡略化したもの。旧UI向けに「画面下の共有ボタンからでもOK」の注記つき）。
  iPhoneのSafariは**7日間使わないと保存データを自動削除する**が、ホーム画面から開く場合は対象外になる。
  **ただしiOSのホーム画面アプリ（standalone）はブラウザとは別の保存領域で動く**（localStorage非共有）＝
  追加しただけでは履歴は空。案内モーダルに正しい順番を明記する:
  ①引き継ぎURLを作る→②ホーム画面に追加→③アプリ側で引き継ぎURLを貼り付けて読み込む（1回だけ。以降はrefreshMyListで自動同期）。
  共有URLのリンクがホーム画面アプリで開かないのもOS仕様（リンクは常にブラウザで開く）で、これも同じ同期がカバーする。
  すでにstandalone起動中（`isStandaloneApp()`）なら案内`#hist-tip-install`を隠す。
  **過去の試合の注意書き・引き継ぎURLカードの文言は短く保つ**（2026-08-07に「丁寧だが長すぎて離脱する」と
  指摘され半分以下に圧縮。1行目=メリット、2行目=やること、の順。詳細な説明はお知らせ記事が担う）。
- **引き継ぎURL（第2段階・端末をまたぐ履歴。2026-08）**: 端末ごとに専用ID（**14文字**・セッションIDと同じ文字種）を発行し、
  `mylists/{ID}` に**参加したグループのID一覧だけ**を置く（`groups/{sessionId}: {at, name}`。本体はsessionsのまま二重に持たない）。
  `#my=ID` のURLを開くと別端末でも履歴一式が戻る＝会員登録なしの引き継ぎ。IDを知る人だけが読める（グループ共有URLと同じ思想）。
  - UI: 過去の試合画面のカード`#mylist-card`（`renderMyListCard`。未作成=「引き継ぎURLを作る」＋**貼り付けて読み込む入力欄**
    （`importMyListFromInput`。URL全体でも14文字IDだけでも可）／作成済み=URL表示＋コピー。
    **自分専用・人に送らない**の注意書き必須）。作成は`createMyList`（衝突チェック付き・現在の履歴を初期投入）。
    貼り付け入力は**ホーム画面に追加したアプリの唯一の取り込み導線**（standaloneはアドレスバーが無くURLを直接開けない）。
    **standaloneでは貼り付け欄を主役にした表示に切り替える**（見出し「ブラウザで使っていた履歴を引き継ぐ」・新規作成は控えめな副ボタン。
    アプリ側でここに来る目的の大半はブラウザの履歴を持ってくることだから）
  - **ホームの入り口は履歴0件のとき無条件に出す**（`renderRecentGames`の「履歴の引き継ぎ」セクション）。
    履歴0件で隠すと、保存領域が空から始まるホーム画面アプリでは引き継ぎ欄のある過去の試合画面へ一生たどり着けない。
    **`isStandaloneApp()`は端末によって外れる**（＝判定に頼ると「入り口が表示されない人」が出る。実際に報告あり）ため、
    **判定は文言の出し分けだけに使い、入り口そのものは判定に依存させない**:
    作成済み=「過去の試合・引き継ぎURL ›」／standalone=「ブラウザで使っていた履歴を引き継ぐ ›」／
    それ以外=「別の端末の履歴を引き継ぐ ›」。履歴が1件でも入れば「最近の試合」に置き換わって消える。
    タイトル`.rg-title`は状態で書き換えるので、履歴ありに戻ったら「最近の試合」へ復元すること
  - `isStandaloneApp()`は `navigator.standalone`（iOS）／`document.referrer`の`android-app://`／
    display-mode の standalone・minimal-ui・fullscreen・window-controls-overlay をすべて見る。
    それでも外れる前提で使うこと（上の無条件表示が最後の砦）
  - **`renderMyListCard`の主役の出し分けも判定ではなく履歴の件数で決める**（未作成のとき）:
    **履歴0件＝持ってくるものが無い＝引き継ぎたい**ので貼り付け欄を主役（見出しも「引き継ぐ」）、
    履歴1件以上なら従来どおり「引き継ぎURLを作る」を主役にする。
    判定で出し分けると、判定が外れたアプリで「案内は引き継ぐ・実UIの主役は作る」と食い違い、
    誤タップで空のリストを作ってしまう（入り口と着地の主役を必ず一致させること）
  - **作成済みカードにも乗り換え用の貼り付け欄**（details「別の引き継ぎURLを読み込む（乗り換え）」）。
    無いと、standaloneで誤って「新しく作る」を押した瞬間にブラウザ側URLへ乗り換える手段が消えて詰む
    （standaloneはアドレスバーが無く#my=URLを開けない。openMyListFromUrlは乗り換え対応済みなのでUIさえあれば戻れる）
  - **過去の試合の注意書き`#hist-tip-text`は状態で切り替える**: 未作成=「この端末だけに保存」／
    作成済み=「引き継ぎURLにも自動で追記」（作成済みなのに「この端末だけ」では直下のカードと矛盾する）
  - 同期: `saveToLocalHistory`→`syncGroupToMyList(g)`（1ページ表示につきグループごと1回だけ書く。`myListSynced`ガード。
    **別のリストに乗り換えたら`myListSynced.clear()`必須**＝忘れるとローカル側のグループが新リストに追記されない）。
    逆方向は`refreshMyList()`＝**起動時と過去の試合を開いたとき**にサーバーのリストから静かに取り込む
    （ブラウザ⇄ホーム画面アプリのように保存領域が分かれていても、双方が開くたびにそろう）
  - 復元・マージの本体は`mergeMyListData(data)`（openMyListFromUrlとrefreshMyListで共用）＝リストを取り込み→
    無いグループをsessionsから並列取得（新しい順・最大MAX_HISTORY件。**取り込んだゲームのプレイヤー名は
    「登録名」にも反映**＝この端末で作成した場合と同じ扱い。引き継ぎ後のセットアップで過去のメンバー名が候補に出る）→
    ローカルにだけあるグループはサーバー側へ追記（双方向）。openMyListFromUrlは**処理後にhashを消す**（再読み込みで二重取り込みしない）
  - **起動時のURL判定は #my= → セッションID（8文字以上）→ アンカー の順**。my=は8文字以上の条件も満たすため、
    先に判定しないとセッション参加に吸い込まれてエラーになる
  - **database.rules.jsonにmylistsノードを追加済み**（rules変更はコンソール手動貼り付けが必要 → docs/site-spec.md）
- **「この試合での自分」と「自分の成績」（2026-08〜）**: アプリは本人を特定できないため、**本人が卓ごとに自分の名前を選ぶ**設計。
  分類はlocalStorage `mahjong-tags-v1`＝`{gid:{role:'play'|'watch', me:index}}`（既定=自分未選択。既定は保存しない。保存キーは変えない）。
  - **画面に「参加／観戦」の区別は出さない**（2026-09-26。わかりにくいと指摘された）。`role:'watch'` は画面では**「自分は出ていない」**
    （記録係・見ていただけの試合＝成績に入れない）。見るだけの人は「見るだけのリンク」（下の節）で開くので、そもそも一覧に入らない
  - UI: 過去の試合の行の「自分の結果」（そのゲームでの順位・スコア）／未選択は点線の「自分を選ぶ」（`.gm-pick`）→`openTagEditor`
    （form-modal「この試合での自分」。名前チップ＋「自分は出ていない」「まだ選ばない」）
  - **引き継ぎURLにも同期**: `mylists/{id}/groups/{gid}` に `role`('play'|'watch')と`me`(0-39)。
    **rules.jsonに両キー追加済み（コンソール貼り付けが必要）**。ルール未適用でも本体同期が壊れないよう、
    at/nameを先にupdateし、role/meは別update・失敗は握りつぶす（分類は端末に残り次の機会に再送）。
    復元（`openMyListFromUrl`）はサーバー側タグを取り込み、サーバーに無い分は端末側から上げる
  - **自分の成績（詳しい表）**（`view-mystats`。マイページ「個人」の成績の「詳しい成績表 ›」から）:
    参加かつ自分設定済みのグループを横断集計（`collectMyStats`）。**四麻/三麻は別列**（着順の意味が違うため）。
    同点は`rankGroups`按分＝個人成績と同じ数え方。行=ゲーム数/試合数/平均着順/1..4着/トップ率/連対率/ラス回避/
    通算スコア/最高・最低点数＋グループ別内訳（タップで結果詳細）。
    スコアは各グループのルールで計算された値の合算である旨を注記する。
    **表の長文対策**: 項目列は**78px**（「通算スコア」=5文字が収まる幅。64pxでははみ出す。行名は「ゲーム数」）。
    グループ名は`.ms-gname`（内側span）で**3行打ち切り＋折り返し**・全文はtitle属性
    （`.rounds-table td.sticky-col`の`white-space:nowrap`を打ち消す。clampはtdでなく必ず内側のspanに掛ける）。
    「チーム戦」は形式列の9pxサブ行（`<div>`2つ構成＝画像側でメイン＋subsとして描かれる）。
    画像出力=`buildMyStatsCanvas`（帯「自分の成績」＋2表）。表HTML→Canvasの共通部は`imgTableHtmlCanvas`
    （imgTableBodyCanvasから抽出。**新しい表を画像化するときはこれを使う**）。
    グループ名なしの画像は`openImagePreview`/`shareCanvasImage`がタイトル・ファイル名を「ラベルのみ」にする
- **セッションIDは`newSessionId()`で発行**（生成→既存チェック→被っていたら作り直し、最大5回。誕生日問題による他人のセッション上書きを防ぐ）。
  初期のIDは22桁だったため、既存データには22桁IDも混在する（セキュリティルールは両方許可）
- **起動時のセッション判定は「ハッシュ8文字以上」**（ページ内アンカー #howto をセッションIDと誤認しないため）
- `settings`: playerNames（3〜40人=MAX_MEMBERS）/ numPlayers（3|4）/ startPoints / returnPoints / uma[] /
  rate・chipRate（**旧キー。画面では使わない**。新規は 0 / 1）/ bonusEnabled（チップ記録）/ startChips（持ちチップ）/ yakitori /
  chombo / chomboPenalty（罰符pts・0=マークのみ）/ teamMode / teams=[{name, members:[playerNamesのindex]}]
- `rounds[]`: points[] / scores[]（チョンボ罰符適用後）/ members[]（参加者index）/ chips[]（**チップ差**）/ yakitori[] / chombo[] / at / edited
  ／ id（その試合を追加した操作のID。2026-09以降の試合のみ）／ op（最後に修正した操作のID）
- `log[]`: type（add / edit / delete / **settings**）／ at ／ no（試合番号）／ points・prev・names ／
  op（その操作のID＝二重適用の判定に使う）／ settingsは label ＋ changes[]（「ウマ: 10-30 → 10-20」のような変更点の行）。
  旧形式の設定変更・「元に戻す」は type:'edit' で no が無い（`logKind(e)` が 'settings' に分類する）
- チョンボ: `applyChombo(scores, chomboArr, s)` が罰符ptsを減算。**submitRoundとsaveGameEditの両方で必ず通す**。焼き鳥はマークのみ
- **名称の表記は「ゲーム名」に統一**（2026-08-08。旧「グループ名」。セットアップのラベル・検索placeholder・
  CSVメタ行・成績シートの行名「ゲーム数」・LP・privacy/terms・news記事まで一括変更。
  変数名`MAX_GROUP_LEN`や保存キーは互換のため据え置き。「グループ」という概念語（グループを作成・共有URL等）はそのまま）
- **名前の文字数上限**: プレイヤー名=`MAX_NAME_LEN`**16**／チーム名=`MAX_TEAM_LEN`**40**／ゲーム名=`MAX_GROUP_LEN`**40**。
  プレイヤー名だけ短いのは表の列見出し（1列58px）になるため。チーム名はcolspanでまとまる／順位カードは1行使えるので長くできる。
  **40文字のチーム名は折り返し前提**で、伸びすぎないよう3行で打ち切る場所がある（表見出し`.team-head-name`・順位カード`.score-name`。
  どちらも`-webkit-line-clamp:3`。thをflex/boxにすると表のセルでなくなるので必ず内側のspanに掛ける）。
  全文はtitle属性・設定カード・結果画像で読める（**画像出力はtextContentを読むので打ち切りの影響を受けない**）。
  入力欄の`maxlength`＋保存時の`clampName`（**書記素クラスタ単位**で切るので絵文字が割れない）の二段構え。
  かける場所はセットアップ／設定変更モーダル／チーム編成エディタ／過去の試合の編集の全入力。
  **既存データに上限超えの名前があっても切り詰めない**（表示は従来どおり＝互換性を壊さない）。
  サーバー側の上限はdatabase.rules.jsonのname≤120・playerNames各≤60（こちらは変更していない）
- **規模の上限**（増えすぎると成り立たなくなるものに置いた線）:
  メンバー`MAX_MEMBERS`40人／1グループの試合数`MAX_ROUNDS`**500**／履歴ログ`MAX_LOG`**600件**（古いものから捨てる`appendLog`）。
  試合数の根拠は①保存のたびに全試合ぶんを書き直すので通信が重くなる（4人500試合で約85KB・1000試合で約168KB）
  ②試合結果の画像が読める大きさの1枚に収まらなくなる ③rules.jsonのrounds indexが4桁まで。
  上限に達したら**新規入力だけ**を止める（既存試合の修正・削除は常に可能）。残り20試合で予告のトーストを出す。
  チーム数に別途の上限は置かない（1チーム1人が下限なのでメンバー数＝40が実質の上限。TEAM_COLORSも40色）
- **互換性ルール（最重要）**: 旧データはキー無し=無効・0枚・1倍扱い。members無しの旧ラウンドは先頭numPlayers人参加とみなす（`roundMembers(r,s)`に集約）。保存キー名・関数名はbonus系のまま変えない（settings.rateも不変）

## 主要関数マップ
- **画面遷移と戻るボタン**: 左上の`#header-back`（36px円形・ゲーム設定と同仕様）を全画面に展開。
  戻り先は`backTargetOf(name)`のマップ＝setup/history→home ／ mystats→history ／
  detail→開いた場所（`detailBackTo`: history or mystats。viewDetailで記録、詳細内の再描画では上書きしない）／
  game→goHome（保存とリスナー停止を通す）／ home・share（作成完了の順方向フロー）には出さない。
  **ホーム画面に追加したアプリ（standalone）はブラウザの戻るボタンが無い**ため、画面内に必ず戻り道を置くのが原則。
  popstate（Androidの戻るボタン・ブラウザの戻る）では ①開いているモーダル・シートを全部閉じる
  （下だけ画面が変わる事故防止）②対局画面から離れるならleaveGame相当の後始末（リスナー停止・pendingSessionId退避）を通す
- 同点処理は `rankGroups(values)` に一元化（ウマ・オカ按分・平均順位すべてこれ）
- 集計: `gameTotals` / `playedFlags` / `chipTotals`。チップ基準値は `chipBaseline(m)` = 持ちチップ + 過去試合のチップ差累計（修正時は対象試合より前まで）
- 結果カード: `scoreMainHtml(g)`（ゲーム=#score-main-body、詳細=`renderDetailBody(g)`。`viewDetail`=そのラッパ。チップ差・焼き鳥・チョンボの回数は個人成績の行）
- チーム: `normalizedTeams` / `teamStandingsHtml` / `memberDisplayOrder`（チームA→B…の表示順）/ `teamColorOf` / `teamDotHtml`。
  編成エディタは `teamEditorCtx` でセットアップ・設定変更モーダル共用。`syncTeamsWithIds`が同期を一元処理
- **チーム戦の制約**: 同チーム同士は対局不可。最低チーム数=1試合の人数（四麻4・三麻3、`teamEditorCtx.minTeams`）。
  ガード: toggleSheetMember / submitRound / defaultParticipants
- ログ: `renderLogModal`（フィルタlogFilter: すべて/入力/修正/削除/**設定**）/ `renderLogList(log, g)`。新規ログは`e.names`に参加者名を保存。
  設定タブは settings（と旧形式の設定変更）を出し、`e.changes` を箇条書きで見せる
- 保存: **`enqueueOp(op)` だけを使う**（→ 下の「送信キュー」）。`sessions/{id}` へ直接 update/set してよいのは作成時の `createGame` だけ
- 画像出力とプレビュー: `imgSectionBodyCanvas` / `buildResultCanvas` / `buildComposedCanvas` / `openImagePreview`（→ その他の機能）
- カード開閉: `collapsedCards` + `toggleCard(key)`（key: team/score/rounds/rank/chart/ichart/settings）
- 計測: `track(event, params)` → dataLayer（group_create / group_join / round_submit / share_copy）

## 送信キュー（入力を失わない・他の人の入力を上書きしない。2026-09〜）
- **背景**: 「入力完了で自分の画面には出たのに他の人には届かず、再読み込みしたら消えた」事故。原因は
  ①保存のたびに手元の rounds 配列を丸ごと書いていた（圏外で古い画面の端末が復帰すると、他の人の試合を上書きして消す）
  ②Firebaseの送信待ちはメモリにしか無い（送る前に再読み込み・アプリを閉じると入力ごと消える）の2つ
- **仕組み**: 1回の操作を op（kind: add / edit / delete / settings / rename / restore）として
  ①端末の送信キュー（localStorage `mahjong-outbox-v1`）に積む →
  ②画面は `overlayPending(serverGame, sid)`＝サーバーの値に未送信opを重ねて表示（`activeGame`はこれ。サーバーの生の値は`serverGame`）→
  ③`flushOutbox` が積まれた順に `sendOp` で `sessions/{id}` 全体をトランザクション（applyLocally=false）→
  ④確定したらキューから消す
- **中身は純粋関数 `applyOp(game, op)`**（トランザクション・重ね表示・tests.htmlで共用）。戻り値は
  apply／already（適用済み＝再送・二重送信）／conflict（他の人の変更とぶつかった）。
  **二重登録しない**: logに op のIDが残っていれば already（追加は試合のidでも判定）
- **ぶつかったときは上書きしない**: 修正・削除は「そのとき見えていた中身の指紋」（`roundTarget`＝id or at ＋ `fpOf(roundCore(r))`）を持ち、
  他の人が先に変えていたら反映せず知らせる（入力した点数をトーストに添える）。
  設定・名前の変更は「編集を開いた時点の値」（`editBase`）の指紋を持ち、変わっていたら反映しない。
  設定の編集中に他の人が先に変えたときは、保存ボタンの時点で気づいて開き直す（サーバー側でも applyOp が二重に確かめる）
- **スコアは適用するときにサーバーのその時の設定で計算する**（op には点数・参加者だけを持たせる）。
  入力中にメンバーが並び替え・追加された場合は `remapMembers` が名前で付け直す（名前の変更は人数が同じなら番号のまま）
- **送る契機**: `.info/connected` がtrueになったとき・`online`・画面に戻ったとき（visibilitychange）・未送信があるあいだ15秒ごと・起動時。
  つながっているのに45秒返ってこないトランザクションは打ち切って送り直す（同じopは二重に適用されない）
- **見せ方**: 画面上部の帯 `#sync-bar`（ヘッダー直下・sticky）。
  オフラインで未送信あり＝「未送信 N件：電波が戻ると自動で送信します（画面を閉じても消えません）」／
  オンラインで2.5秒以上送れない＝「送信中… N件」／権限・検証エラー＝「送信できなかった入力」＋もう一度送る・破棄／
  未送信なしでオフラインが4秒以上続く（ゲーム画面のみ）＝「オフラインです。他の人の入力は、電波が戻ると表示されます」。
  送れていない試合の行には「未送信」印（`.pend-mark`。1.2秒たってから見えるCSSアニメーション＝普段はちらつかない）。
  帯を出したあと全部送れたら「未送信だった入力を送信しました」
- **権限・検証エラーは捨てない**（`failed` を付けてキューに残し、帯で知らせる）。何度送っても通らない種類なので自動再送はしない
- **【最重要】`sessions/{id}` の直下に新しいキーを足さない**。トランザクションはゲーム全体を書くので、
  database.rules.json の `$other: false` に引っかかると**本番の保存が全部失敗する**。スタブ（smoke_test等）にはルールが無いので気づけない。
  保存まわりを触ったら **`python tools/sync_test.py`（本番DBで2台の端末を再現・検証用ゲームは自動削除）** を必ず通す
- 設定変更の変更点は `describeSettingsChanges(base, next, editMembersState)` が作る
  （ゲーム名／メンバー追加・名前変更・削除・並び順／持ち点／返し点／ウマ／チップ記録・持ちチップ／焼き鳥／チョンボ・罰符／チーム戦・編成）。
  何も変わっていなければ保存しない（「変更はありませんでした」）

## クイックスタート（LPの「はじめる」の行き先）
- **LPのCTA「はじめる」は `openQuickStart()`**（ヒーロー・クロージングの2箇所とも）。
  人数の2択モーダル `#quick-start` を開く → `quickStart(4)` / `quickStart(3)` で
  **セットアップ画面も共有画面も挟まずゲーム画面へ直行**する。
  「LPからすぐ点数入力にたどり着けない」ことが利用のハードルになっていたための導線（2026-08-26）
- 作られるグループ: ゲーム名=`todayStr()`（今日の日付）／メンバー名=`placeholderNames(n)`の**仮名A・B・C…**／
  設定は**セットアップ画面の初期値と完全に同じ値**（四麻25,000-30,000・ウマ10-30／三麻35,000-35,000＝オカなし・ウマ10-0-10、
  `rate: 0`・`chipRate: 1`（旧キー）・持ちチップ20・チップ/焼き鳥/チョンボはオフ・罰符20）。
  **ここを変えるときはinitSetupFormと必ず両方直す**（片方だけ変えると入り口によって初期設定が変わる）
- **三麻の既定は 35,000点持ち／35,000点返し（オカ0）／ウマ10-0-10**（2026-09-17〜。以前は30,000/35,000でオカ15pts）。
  この数字は記事・FAQ・SNS画像にも書いてあるので、変えたら docs/blog-spec.md の一覧にある記事と
  tools/ の画像スクリプトも一緒に直す。`tools/smoke_test.py` のステップ7が初期値を固定している
- 仮名は `savePlayerNames` に渡さない（`createGame(..., {saveNames:false})`）。
  A・B・C…が登録名の候補に混ざるのを防ぐため
- モーダルには「メンバー名やルールを先に決める ›」でセットアップ画面への導線を残す（従来フローは廃止しない）
- **作成処理は `createGame(name, settings, opts)` に一本化**。`startGame`（セットアップ画面から）も
  `quickStart`（LPから直行）もここを通る。`opts.toGame`=true なら共有画面を挟まずゲーム画面へ
- 試合形式（三麻/四麻）は作成後に変更できない（`settingsFormHtml`の注記どおり）。
  それ以外（ゲーム名・メンバー名の変更/追加・チーム戦・持ち点/返し点/ウマ・チップ・焼き鳥・チョンボ）は
  ゲーム画面の「設定の編集」からいつでも変えられる＝**仮のまま始めて後から整える**前提の設計
- **ゲーム画面のタイトル右に「URLを共有」ボタン**（`copyUrl()`）。
  クイックスタートは共有画面を通らないので、共有URLを取る手段をここに置く必要がある
  （ホーム画面に追加したアプリはアドレスバーが無く、他にURLをコピーする方法が無い）
- 試合0件の「試合結果」には次の一手を書く（「下の『＋ 点数入力』から1試合目を記録できます」）。
  作りたてのグループで最初に目に入る場所のため
- **この動線を変えたら `tools/smoke_test.py` も直す**。LPのCTAを押した先がセットアップ画面から
  2択モーダルに変わったとき、テストが `#s-name` を探して失敗しCIでデプロイが止まった（2026-08-26）。
  テストは「2択モーダル →『先に決める』→ セットアップ」の詳細動線と、クイックスタート直行の両方を通る

- **タイトル横の「URLを共有」「設定の編集」は白地＋枠線**（`.title-edit-btn`。2026-08-26）。
  以前は薄い塗り #e0f2f5 だけで、画面の背景 #DBF4F9 とのコントラストが**1.01＝ほぼ同色**のため
  ボタンの輪郭が見えなかった。白地＋1.5px枠線で面を作り、文字は12px・高さ32pxにしてタップしやすくした。
  三麻は文字と枠線を #4261AA に切り替える（`#view-game.sanma` / `#view-detail.sanma` の両方に指定）。
  **薄い塗りだけの配色に戻さないこと**（背景色が近いので必ず埋もれる）

## 設定画面の注意書きの方針（2026-08-26〜）
- **UIが既に防いでいる制約は文章で説明しない**。読まれないうえ画面が重くなる。削った例:
  試合形式は変更できない（変更UIが無い）／対局済みメンバーは削除できない（`canDelete`で削除ボタンを出さない）／
  チーム数の下限（`minTeams`で下回れない）／チョンボ罰符0はマークのみ（選択肢が「0（マークのみ）」）
- 残す注意書きは**1行に収まる長さ**にする。`.chips-hint` は 15件663字 → 11件274字に整理した
- 新しく制約を足すときは、まず**UIで防げないか**を考える。文章はその次

## チップまわりの扱い（倍率・換算は2026-09-25に廃止）
- **スコア倍率・チップ倍率・換算（倍率を掛けた値）は、アプリから機能ごと取り除いた**。
  セットアップ・設定の編集に倍率の欄は無く、結果画面に換算の表示も無い。AdSense審査で
  「賭け麻雀のレート表に見える」ことを避けるため（→ docs/site-spec.md「ギャンブル判定を避けるための線引き」）
- **旧データの `settings.rate` / `chipRate` は読み込むだけ**。どこにも表示せず、設定の編集では`...s`でそのまま持ち越す
  （書き換えない＝データ互換性を維持）。新しいグループは `rate: 0` / `chipRate: 1`
- 残っているのは**チップの枚数の記録**（`bonusEnabled`＝「チップあり🍪」・持ちチップ `startChips`・試合ごとのチップ差 `round.chips`）。
  総合順位のミニトグル「スコア/チップ」と、個人成績の「チップ差」行で見せる
- 個人成績の末尾に、記録しているものだけ **チップ差／焼き鳥／チョンボ** の行（行ラベルは文字だけ）を出す（旧データは記録があれば出す）。
  最初の1行の上に区切り線（`firstExtra`）
- **保存キー・CSSクラス・JS変数は据え置き**: `bonusEnabled` `chipRate` `startChips` `.score-bonus` `#s-bonus` `#e-bonus` など。
  画面に出ないうえ、保存キーを変えると既存データが読めなくなる
- **倍率・換算を復活させないこと**。「レート」「換算」「×500」のような語・数字をUI・JSのコメントにも書かない（生HTMLは審査で読まれる）

## セットアップ画面
- 統合カード（グループ名 → メンバー名 → **試合形式**=四麻/三麻＋個人戦/チーム戦の2トグル1ラベル）→ ルール設定 → オプション
- プレースホルダーは例の値をそのまま（まじゃすこ学園サッカー部／山田先輩／150等。「（例）」は付けない）。
  **ゲーム名の初期値は今日の日付**（`todayStr()`＝「2026.08.08（土）」形式。initSetupFormでセット。
  そのまま作成可・書き換え自由。全部消したまま作成してもstartGameが日付名にフォールバック）
- **作成日はゲーム画面・結果詳細のタイトル下に自動表示**（#game-date / #detail-date）
- 「登録名の管理」ボタンはメンバー名ラベルの右（常時表示）。モーダル: [ゲームに追加]/[取り消し]、右上[編集]で↑↓並び替え・削除
- **三麻選択でview-setupに.sanma**（トグル・カード枠が青#4261AAに）
- **オプションカードはデフォルトで閉じた状態**（右上の▸/▾トグルで開閉。`toggleSetupOptions`。画面を開くたびinitSetupFormで閉じ直す）
- オプション名: 「チップあり」「焼き鳥あり」「チョンボあり」（名前の後ろにアイコン i-chip / i-yakitori / i-chombo）。説明は「本文。<br>※注意書き。」の2行形式。
  チップのヒントは「試合ごとにチップを入力でき、増減を記録します。」
- 「チップあり」をONにすると「持ちチップ（枚）」の欄だけが出る（初期値20）
- チョンボ罰符セレクト: 0/−10/−20/−30/−50/カスタム。デフォルト−20pts
- チーム編成エディタ: 各ボックスにチームカラー（左5pxボーダー＋●）。タップで次チームへ・ランダム編成（i-dice）・チーム名編集

## スコア入力シート
- **「設定の編集」と同じ中央モーダル形式**（.sheet=暗背景オーバーレイ、.sheet-box=白パネル max-width:430px・パネル内スクロール・transformなし・背景タップで閉じる）
- **パネルは上端固定**（.sheetはalign-items:flex-start＋padding-top:6vh。縦中央揃えにしない）:
  対局メンバーの選択人数で中身の高さが変わっても上側の位置は動かず、下だけが伸縮する（メンバーチップの位置が上下にぶれない）
- タイトル右に小「キャンセル/完了」＋最下部に「入力完了/キャンセル」（#confirm-btn-topは下と活性同期）。タイトルに試合番号
- **列順は左から 名前｜焼き鳥｜チョンボ｜チップ｜点数**。入力欄・ボタンは固定幅で右詰
  （焼き鳥/チョンボ42px・チップ68px・点数104px・gap6px）、名前が残り幅（可変・ellipsis）
- オプションが1つでも有効なら列見出し行を表示。**見出しは文字（「焼き鳥」「チョンボ」）、ボタンはアイコン（`ico('yakitori'/'chombo', 22)`）**。
  OFFは灰色の変数（filter・opacityは使わない）、ONは焼き鳥＝主色の地・チョンボ＝赤い地（`.yaki-btn.chombo`）。
  ONは右上のチェックと `aria-pressed` でも示す（色だけに頼らない。toggleSheetYaki/Chomboが両方更新する）
- **整数のみ**（blockNonInt。マイナスは箱下用に許可）。点数placeholderは持ち点連動（startPoints/100）。チップは「現在のチップ数」を入力し差分保存
- バリデーション: 点数合計=持ち点×人数 かつ チップ合計=基準合計。**チェック表示はチップが上・点数が下**（同サイズ13px）
- 入力途中値はメンバーindexキーで保持（sheetPrefill/sheetChips/sheetYaki/sheetChombo）
- **新規入力は「誰も選ばれていない状態」で開く**（メンバー数>1試合の人数のグループだけ。`openSheetCtx`の`canChoose`）。
  自動で選んでおくと、違う顔ぶれのときに「まず外してから選び直す」二度手間になり、
  定員に達した状態で別の人を押すと「先に外してください」で操作が止まるため。
  **人数ぴったりのグループは従来どおり全員を自動選択**（選択UI自体を出さないので、空にすると入力できなくなる）。
  修正時はその試合のメンバーを選択した状態で開く（従来どおり）
- **「前回と同じ」ボタン**（`#sheet-same-btn`・`selectSameAsLast`/`lastRoundMembers`）で直前の試合の顔ぶれを1タップ復元。
  出す条件は**新規入力のときだけ**（修正中の「前回」は最後の試合を指して意味がずれる）かつ
  直前の試合のメンバーが今も全員在籍していて人数が合う、かつ現在の選択と違うとき。
  **`lastRoundMembers`はチーム重複も見る**（`defaultParticipants`のdistinctTeams相当）:
  直前の試合の後にチーム編成を変えて同チーム同士になっていると、押した瞬間に
  「同じチームのメンバー同士は対局できません」で詰む組み合わせを復元してしまうため、その場合はボタンごと出さない
- 未選択で開く仕様にしたので、**`autoFill`は`sheetOrder.length !== numPlayers`のとき何もしない**
  （入力欄自体が存在せず`si-0`がnullになる。ボタンはdisabledだが二重に守る）
- チーム戦では対局メンバーチップもチームA→B…順＋チームカラー●
- 対局メンバー選択欄に**独自スクロールは付けない**（全員分を表示し、パネル全体のスクロールに任せる）

## 結果表示（ゲーム画面・結果詳細共通）
- カード順: チーム順位（チーム戦のみ）→ 総合順位 → 試合結果 → 個人成績 → スコア推移 →
  個人スコア推移（チーム戦のみ）→（詳細のみ）設定
- 全カード右上に開閉トグル（▾/▸）。開き直すと全開にリセット。たたむとアクションボタンも隠す
- **0試合の間は順位バッジを出さない**（灰色「—」。1試合目から順位・メダル表示）
- 総合順位: pts順位がデフォルト。ミニトグル「スコア/チップ」はチップあり🍪のときだけ。
  チップ差・焼き鳥・チョンボの回数は個人成績の行で見せる（総合順位カードに内訳セクションは無い）
- 試合結果（旧「試合結果」。2026-08-06に改称。外部ページの言及も同時に直した）: 常に表形式。行タップで修正・削除。
  **最終行に合計行**（`roundsTotalRowHtml(g)`。画面・画像で共用）。表示を直近n試合に絞っていても**合計は常に全試合の累計**＝総合順位の値と一致する。
  合計行には`total-row cat-break`を付ける（`cat-break`は「数字の意味が変わる境目」の線で、画面のCSSと画像の`c.catBreak`が同じ線を引く）。
  全体を見たいときは**画像プレビュー**（下記）。
  **列幅・先頭列の体裁は個人成績と完全に統一**（メンバー列58px。上下に並ぶ2表の列位置がそろう）。
  先頭列幅は`firstColW(forImage)`: **画面64px（小さめ・最長ラベル「平均着順」「メンバー」=48pxがぎりぎり収まる幅）／画像出力86px**。
  試合番号の列は項目列と同じ`.sticky-col`（12px・weight600・標準色・左詰め・横スクロール時固定）
- **見出し行の先頭セルは「列の見出し」ではなく「行の見出し」**（転置レイアウトなので、その行が何の行かを示す）:
  プレイヤー名の行＝**「メンバー」**（試合結果・個人成績とも。旧「試合」「項目」）／チーム名の行＝**「チーム」**。
  4文字がちょうど収まる幅なので、これより長いラベルにするなら`firstColW`も広げること
- 個人成績: **試合結果と同じ転置レイアウト（列=メンバー・行=成績項目、1行目にプレイヤー名）**。項目列はsticky。
  行=上から **試合数/平均着順**（全体像）→ **1着..n着**（内訳）→ **トップ率/連対率/ラス回避**（率。トップ率と
  ラス回避の間に連対率）→ スコア/素点/順位点/最高点数/最低点数（平均点数は載せない）。
  **`statLabels`とセルを積む順は必ず一致させる**（`rankCountHtml`。片方だけ直すと値と項目名がずれる）。
  試合数も太字（数値の行はすべてweight700で統一）。
  **回数に単位「回」は付けない**（試合数・1着..n着とも数字だけ。`fmtCount`。焼き鳥・チョンボの行も同じ）。
  **数字の意味が変わる境目に区切り線**（`tr.cat-break`＝border-top 2px #ccd8d5。通常の行罫線1px #eeeより少し太く濃い）:
  個人成績はラス回避（率）→スコア（点数系）の間と、点数系→チップ差・焼き鳥・チョンボ（記録）の間（先に出る方1行だけ）。
  画像側は`c.catBreak`を見て同じ線を描く
  素点=(持ち点−返し点)÷1000合計、順位点=スコア−素点、連対率=(1着+2着)÷試合数、トップ率=1着÷試合数。
  ベスト（平均着順min・連対率/トップ率/ラス回避/最高点数max）は文字色オレンジのみ
- **チーム戦のチーム名行**: 試合結果・個人成績の両表とも、プレイヤー名行の上に`teamNameRowHtml`のチーム名行を置く
  （チームごとにcolspanで1セル・プレイヤー名と同じ文字サイズ・TEAM_HEAD_BG背景）。
  チームの境目は縦区切り線クラス`.tb`（**1px**=本文の横罫線と同じ太さ。見出し=白 rgba(255,255,255,.85)・
  本文=薄グレー #e3eaea）をチーム名行/名前行/本文の全セルに付けて示す
  （境目列の算出は`teamBoundarySet`、グループは`teamGroupsOf`）。
  **同じ線を2か所にも引く**: ①先頭列（試合番号・項目）の右 ②チーム名行の下（`tr.team-row th`のborder-bottom）。
  ①は`border`ではなく`box-shadow: inset -1px 0 0`で描く（border-collapse:collapseの表では罫線が表側に属するため、
  横スクロールするとsticky列の罫線だけ取り残される）。①はチーム戦・個人戦の両方で出す。
  **②のうち「チーム」セル（sticky列）だけは同じ理由でborderが効かない**（sticky列は`z-index:1`で自分の背景を
  collapseされた罫線より後に描くため、隣の「チーム名」セルより細く／薄く見える）。
  `.rounds-table tr.team-row th.sticky-col`で**border-bottomを0にしてbox-shadowで引き直す**（右端と下端の2本を指定）。
  上書きは`th`/`td`付きで書く（`.rounds-table th.sticky-col`(0,2,1)にクラス2つ(0,2,0)では勝てない）
  **長いチーム名・プレイヤー名は何度でも折り返す**（`.rounds-table th`にword-break:break-all。列幅を押し広げて表を横長にしない）
- **表示量の調整（規模が大きいときに画面が長くなりすぎないようにする）**:
  試合結果は既定で**直近`ROUNDS_PREVIEW`=20試合だけ**を描き、下の「すべての試合を表示する（全n試合）」で全件に切り替える
  （`visibleRoundIdx` / `roundsShowAll` / `toggleRoundsShowAll`。25試合以下ならボタンを出さず全件）。
  履歴モーダルも既定は**最新`LOG_PREVIEW`=50件**（`logShowAll`）。
  **試合番号とタップ先は常に本来のindex**（表示を絞っても第281試合は281のまま・`openRoundMenu(280)`）。
  **画像・CSVは常に全件**（絞るのは画面だけ）。別のグループ・結果を開いたら`resetViewLimits()`で解除する。
  効果（実測・375px幅）: 4人300試合＝24.3画面分→**4.0画面分**／16人4チーム100試合＝10.9→**6.6画面分**
- **横スクロールの案内**は`.tbl-scroll`＋`updateScrollHints()`。
  **実際にはみ出しているかを測ってから**出すので、少人数やPCの広い画面では出ない（描画後とリサイズ時に判定）
- **カード上の表は自動縮小しない＝横スクロールで見る**（`overflow-x:auto`＋`tableStyleFor`のmin-width）。
  一時期zoomで1画面に収める実装を入れたが、モバイルで文字がセルからはみ出すため撤去した（下の落とし穴参照）。
  「全体を1枚で見たい」ニーズは**画像出力ボタン**が担う
- **長い名前への耐性**（プレイヤー名・チーム名）: 区切りのない半角英数字（`aaaa…`）が最も崩れやすい。
  日本語・全角英数・韓国語などはブラウザが自動で折り返すが、半角の連続は折り返されず横に伸びる。
  対策は2つセットで必要:
  ①折り返しを許す`overflow-wrap: anywhere`（`.score-name` `.team-members-line` `.score-bonus` `.chart-legend span`
  `.sel-chip` `.team-chip` `.rounds-table th/td.sticky-col`）
  ②**flexアイテムに`min-width: 0`**（`.score-info`）。これが無いと最小幅が中身に引きずられ、
  右のスコア値が画面外へ押し出されて見えなくなる。値側は`flex-shrink: 0`＋`white-space: nowrap`で守る
- チーム識別: TEAM_COLORSは**40色**（1-8は赤#e60012/青#0075c2/緑#00a040/橙#f39800/紫#9b26b6/黄#e6b400/桃#ff5ca8/茶#8b5a2b で不変。
  9色目以降も白以外の互いに異なる色）。名前の前に`.team-dot`●（文字色は変えない）。チーム戦の表見出しは灰色TEAM_HEAD_BG=#5f6e6b。
  列はチームA→B…順
- グラフの縦軸グリッド: **目盛りが12本未満になる最小の刻み**を`GRID_STEPS`（50/100/200/500/1000/2000/5000…）から選ぶ。
  「範囲600以上で50→100刻み」はこの規則の一部（600/50=12でちょうど切り替わる）。
  100試合規模だと範囲が数千〜1万になるので、100で止めると目盛りの数字が重なって完全に読めなくなる
- グラフの横軸ラベル（試合番号）は`chartXTicks(R, maxLabels)`で間引く（画面12個・画像14個）。
  末尾は必ず出し、直前の目盛りと近すぎるときはその目盛りを落とす。
  **データマーカー（各試合位置の丸）は描かない＝折れ線だけ**（2026-08。画面SVG・画像Canvasとも。
  試合数が増えると点が重なって線がだんご状になるため、以前は間隔6px未満で消す`chartShowDots`があったが、
  マーカー自体をやめて常に線だけにした。凡例の●と順位カードの●はデータマーカーではないので残す）
- 系列・目盛りの計算は`chartData(rounds,s,indiv)`に集約し、画面のSVG（`chartHtml`）と画像出力（`imgChartBodyCanvas`）が共用する
- グラフ: 個人戦「スコア推移」／チーム戦「チームスコア推移」（チーム合算・チームカラー）＋その下に
  「個人スコア推移」（`chartHtml(rounds, s, true)`。**個人の線の色は表示順（memberDisplayOrder）のn番目の人＝TEAM_COLORSのn色目**。
  個人戦のスコア推移も同じ割当。凡例も表示順に並ぶ）
- 順位バッジ: 1〜3位のみ🥇🥈🥉、4位以下は灰色数字
- タイトル横ボタンは「設定の編集」（.title-edit-btn・三麻で青）
- **「もっと見る」（全画面モーダル）は廃止**（2026-08-06）。全体を1画面で見る用途は画像プレビューが完全に置き換えたため、
  試合結果・個人成績・グラフの4か所すべてから削除し、代わりに「画像」を置いた。
  画像出力用の広い版を作るフラグ名は`forImage`（旧`forModal`）。
  **画像の試合結果は画面と同じくスコアの下に薄い点数・チップ差を出す**（`roundRowHtml(..., forImage=true)`。
  焼き鳥・チョンボの印（SVGアイコン）だけ画像では出さない）、
  **個人成績は`rankCountHtml(…, true)`でtable-layout:auto**（列幅が内容基準になり項目ラベルが省略されない）

## 色とアイコン（2026-09-26〜）
- **色の役割は4つに分けて混ぜない**: ティール #177083（三麻は青 #4261AA）＝操作・選択・見出し・アイコン／
  着順の色＝1試合の「着」／メダル＝人の「位」／紺 #1A237E と赤 #C62828＝スコアの正負・勝ち負け。オレンジ #E75620 はCTAボタンだけ
- **着順の色**（`RANK_COLORS`・CSSの `--rk1〜--rk4`）: 1着ゴールド #D4A017・2着スチール #6E8CA8・3着グレー #B4C0BD・ラス クリムゾン #C92F42。
  三麻は3色で3着＝ラスの色（`rkCls(rank, np)` が r4 を返す）。ブランド色（ティール・三麻の青）とぶつからない値にしてある。
  小さなタイル・チップは1着とラスだけ塗り、2着・3着は淡い地＋縁（トップとラスだけが目に入る）。％・回数の文字は着順の色にしない。
  **赤はその1試合のラスだけ**（3着でスコアがマイナスでも着順の色は赤くしない）。着順の色は画像出力に入れない
- **位＝メダル**（`medalSvg(rank, size, withNum)`・`--md1〜3`・画像の `IMG_MEDAL`）: 金 #D4A017・銀 #9AA5AD・銅 #C97B3F、4位以下は灰色。
  数字は濃い字（金の上の白は2.4:1で読めない。画像の `IMG_MEDAL_TEXT` も同じ）。横に「n位」と書くピルでは `withNum=false`。
  「着」と「位」はどこでも文字で書き分ける（1着のゴールドと1位の金は同じ値）
- **アイコン**: 絵文字は使わない（端末ごとに絵柄が違い、LPのイラストと調子が合わない）。`<body>`直後のスプライト `#icon-sprite`（48×48のsymbol）を
  `ico(name, size, cls)` が `<use>` で呼ぶ。色はCSS変数 `--i1` 主・`--i2` 副・`--i3` 淡・`--ic-neg`（チョンボのマイナス）で、
  三麻（.sanma）・削除（.danger）・OFF（.yaki-btn）は変数の上書きだけで変える（filter・opacityは使わない）。
  大きさはS（12〜18px・文字の前）・M（22px・直径36pxの丸の上）・L（34px・空の状態の挿絵。`ico-l`）。
  **差し色 `--i4` はS・Mでは主色**（オレンジなし）、Lだけ LP と同じオレンジ1点。18px以下で使うsymbolは線の太さ4以上。
  `i-chevron`・`i-more`・`i-close`・`i-search` だけ `currentColor`。`<option>`・Canvas・`textContent` にはSVGを置けないので文字か図形にする

## 対戦成績・「あなたはどれ？」・まとめて選ぶ（2026-09-25〜）
- **対戦成績**＝同じ試合に出た2人の比較。本体は純粋関数 **`h2hAdd(acc, g, keyOf)`**（tests.htmlで検証）。
  `acc[a][b] = {n, w, l, d, rankA, rankB, scoreA, scoreB}`。**勝ち＝その試合で相手より上の着順**
  （`rankGroups`の平均順位で比べ、同じなら引き分け）。点数が欠けた試合は飛ばす（`null`はNumber()で0になるので変換前に見る）。
  keyOf＝メンバーindex→人のキー（null=数えない）。グループ内はindex、グループをまたぐときは名前（`personKey`）
- **グループの「対戦成績」カード**（ゲーム画面`#h2h-card`・結果詳細とも個人成績の下。開閉キー`h2h`）:
  「だれから見る？」チップ（既定＝自分に選んだ人→未選択なら合計スコア1位。`h2hFocus[gid]`）／
  相性がいい相手・手ごわい相手（**3試合以上**一緒に出た相手で勝率60%以上・40%以下。`h2hSummaryHtml`）／
  相手別の表（同じ試合・直接対決・平均着順〔上=自分・下=相手〕・スコア差〔同じ試合での合計の差〕。`h2hRowsTableHtml`）／
  折りたたみの**総当たり表**（行の人から見た勝ち-負け。勝ち越し=薄青・負け越し=薄赤。`h2hMatrixHtml`）。
  プレビュー画像は`buildTablesCanvas`（帯＋［見出し＋表］×n。自分の成績シートと同じ組み方）
- **表の幅はスマホに収める**: 相手別の表は先頭72px＋4列×62px（=320px）。広げると390px幅で「スコア差」が切れて横スクロールになる
- **「あなたはどれ？」**（ゲーム画面の最上部`#askme-card`）: 自分を選んでいない・「自分は出ていない」でもない・「あとで」を押していないゲームで出す。
  割り勘アプリTricountの「開いた人に一度だけ聞く」方式。これまで自分に選んだ名前（`myKnownNameKeys`）が
  このゲームにいれば「あなたは『○○』ですか？ はい／ほかの名前／自分は出ていない／あとで」、いなければ名前のチップを並べる。
  **全員が仮名（A・B…＝`isPlaceholderName`）のゲームでは聞かない**。「あとで」はこの端末でそのゲームには二度と聞かない
  （localStorage `mahjong-askme-skip-v1`・最大300件）。答えは`setTag`＝「この試合での自分」と同じ（引き継ぎURLにも同期）
- **まとめて選ぶ**（過去の試合`#hist-organize`→`openOrganize`）: 自分未選択の参加卓（試合あり・仮名だけの卓は除く＝`untaggedGames`）を一覧にし、
  「あなたの名前」チップ（出てくる卓の多い順。自分に選んだことのある名前が先頭）を押すと、その名前がいる卓を全部自動で選ぶ。
  卓ごとのselectで個別に直せる（選ばない／各名前／自分は出ていない）。保存で一括`setTag`
- **自分の成績シートの「対戦相手別」**（`myH2HCardHtml`・`collectMyH2H(mode)`）: 自分を選んだ参加卓を横断し、
  相手は`personKey`（NFKC・空白除去・小文字）が同じなら同じ人としてまとめる。**仮名A・B…は除外**。
  表記は最新のゲームの名前。三麻と四麻の両方があるときは「すべて/四麻/三麻」を切り替えられる。画像にも入る

## ログイン（Google・任意）とアカウント同期（2026-09-25〜）
- **目的**: iPhoneのブラウザ・ホーム画面アプリ・LINEの中のブラウザは保存領域が別々で、引き継ぎURLを手で移す作業が要っていた。
  ログインすれば、どこでログインしても同じ「自分の記録」（過去の試合の一覧・自分の選択・まとめ）になる
- **仕組みは引き継ぎURLの上に乗せただけ**: `users/{uid}/listId` に mylists のリストIDを1つ持つ。同期の本体は既存の
  `mergeMyListData` / `syncGroupToMyList` / `refreshMyList`。ログインしない人の動きは何も変わらない
- `attachAccount(user)`（onAuthStateChangedから）: ①アカウントにリストが無い＝初ログイン→この端末の引き継ぎリスト（無ければ`createMyList(true)`で作る）を
  アカウントに付ける ②アカウントのリストと端末のリストが違う→アカウントのリストへ乗り換えて取り込み、
  旧リストの卓・まとめは`mergeOldListInto`でアカウント側へ移す ③同じ→`refreshMyList`。最後に`claimAllTaggedSeats`
- ログアウト（`confirmSignOut`→`signOutAccount`）: この端末の`myListId`を外すだけ。端末の履歴もアカウントの記録も消さない
- **認証はリダイレクト方式**（`signInWithRedirect`）。iPhoneのホーム画面アプリではポップアップ方式が動かない。
  **authDomainは本番では`majasco.jp`**（Safariが別ドメインの保存領域を遮るため、firebaseapp.comのままだとリダイレクト後に結果を受け取れない。
  Firebase Hostingは`/__/auth/handler`を自動で配信する）。localhost・web.appはfirebaseapp.comのまま
- **LINEの中のブラウザではGoogleがログインを拒否する**（埋め込みブラウザ禁止）。`IN_LINE_APP`（UAの`Line/`）なら
  ログインボタンを「ブラウザで開く」にして`?openExternalBrowser=1`付きで開き直す
- **`LOGIN_ENABLED`（index.html）がfalseの間はログインの画面を一切出さない**。Firebaseコンソールの設定（docs/site-spec.md「ログイン」）が
  済んでからtrueにする。テスト（tools/account_test.py）は`window.__MAJASCO_TEST_LOGIN__`で有効化してスタブのauthで検証する
- 画面: 過去の試合の`#account-card`（未ログイン=「Googleでログイン」／ログイン中=表示名とログアウト）。
  ログインできる環境では引き継ぎURLのカードを「ログインせずに引き継ぐ（引き継ぎURL）」に畳む。ログイン中は「ログインできない端末で使う」に畳む

## 共有URLと LINE（2026-09-25〜）
- **試合の共有URL（`shareUrlOf`）は `https://majasco.jp/#ID`**。LINEで押すとLINEの中の画面でそのまま開き、点数入力・閲覧はログインなしでできる
- **外のブラウザに出すのは仲間ページの招待リンク（`circleInviteUrl`＝`/?openExternalBrowser=1#join=コード`）だけ**。
  参加にはログインが要り、LINEの中の画面ではGoogleがログインを拒否するため（LINE公式の仕様で、`openExternalBrowser=1` は **#より前**に置く）
- 起動時に`openExternalBrowser`をアドレスから外す（`history.replaceState`。ハッシュは残す）
- ログインが要る場面（ログインボタン・仲間ページの作成/追加/参加）でLINEの中にいたら、ボタンを「Safari・Chromeで開く」（`externalBrowserUrl`）に替える

## マイページとログイン画面（2026-09-25〜・2026-09-26に作り直し）
- **過去の試合・自分の成績はログインした人だけの機能**。ブラウザに残る履歴（localStorage）は、iPhoneのSafariのように一定期間で消えることがあるため、
  画面には出さない。ログインすればアカウント（users/{uid}/listId → mylists）に残り、どの端末でも同じ一覧になる
- **マイページ（`view-history`）の形**: 一番上に自分のカード`#account-card.mp-hero`（`renderAccountCard(st)`。アイコン・名前・ログアウト・
  自分の試合数／過去のゲーム数／仲間ページ数）→ **「個人｜仲間」の切り替え**（`.seg`・`setMpTab`。最後に見た方を localStorage `mahjong-mp-tab`）
  - **個人**（`#mp-me`）: まとめて選ぶの案内（`renderOrganizeCard`・琥珀色の箱）→ **成績**（`mpStatsHtml`。四麻/三麻の切り替え`mpMode`＝小さいセグメント`.seg.xs`。
    主役の数字2つ＝通算スコア（下に1試合平均）・平均着順／3つの率＝トップ率・連対率・ラス回避（目安の縦線付きのゲージ。四麻25/50/75%・三麻33/67/67%）／
    着順の分布（「n着」のタイル・横棒・％・回数の1行ずつ）／最近10試合の着順（いちばん新しいものに枠）／スコアの推移（0より上は紺・下は赤で塗り分け））→
    **よく打つ相手**（`mpRivalsHtml`。同じ試合の多い順に5人。右に勝率・下に `wlBarHtml`＝左の青が勝ち・右の赤が負け・中央の線が五分）→
    **過去の試合**（`renderHistoryList`。iOS風の検索欄＋形式のセグメント（値は hidden の `#hist-mode` が持つ）。**1つの角丸リスト`.gm-list`**。
    1行＝カレンダー型の日付・ゲーム名・形式・メンバー・**自分の結果（そのゲームでの順位とスコア。1〜3位はメダルの色のピル）**。
    右の「…」＝`openHistMenu`（ボトムシート）：結果を見る／続きを入力／共有／この試合での自分、削除だけ別のまとまり。
    既定は15件＋「さらに表示」）→ ホーム画面に追加の案内
  - **仲間**（`#mp-circle`・`renderCircleSection`）: 仲間ページのカード（アイコン・人数・ゲーム数・「あなた 2位 / 5人」（1〜3位はメダルの色）・立場のピル）→ ＋仲間ページを作る →
    「招待リンク・参加コードを持っている」（開くと入力欄）。人数・ゲーム数は`loadCircleCardInfo`で名簿と試合の一覧だけ読む。
    「あなた○位」は仲間ページを開いたときに`saveCircleSummary`が `mahjong-circles-v1` に控えた値
- 成績の見せ方の部品は**マイページ・自分の成績・仲間ページのメンバーで共用**: `statTilesHtml`（主役の数字2つ＋3つの率）/ `rankBarHtml`（着順の分布）/
  `formHtml`（最近10試合・同着は「=」）/ `sparkHtml`（通算スコアの推移。clipPath の id は `sparkSeq` で毎回変える）/ `wlBarHtml`（勝ち負けの棒）。
  色の決まりは「色とアイコン」の節（着順＝`rkCls`、位＝メダル、正負＝紺と赤）。1試合ごとの記録は `collectMyStats` の `modes[np].hist`・`circleStats` の `hist`（{t, o, rank, tie, sc}。`sortHist`で古い順）
- 詳しい表（`view-mystats`）は残す（全項目の表・対戦相手別の全員・ゲーム別・画像）。入り口は「個人」の「詳しい成績表 ›」「すべての相手 ›」
- **見た目の組み方（iOS風・2026-09-26）**: マイページ・自分の成績・仲間ページは淡い地 #F2F6F5 の上に白い角丸のグループ（`.mp-panel`・`.ios-group`・`.gm-list`）。
  区切り線は .5px #D5DEDB を文字の始まる位置から。切り替えはセグメンテッドコントロール（大 `.seg`／中 `.seg.sm`／小 `.seg.xs`）。
  補足の文字は #5F6E6B・11px以上（#8A9AA5 などは白の上で4.5:1に届かない）。CTAではない操作は淡いボタン `.btn-tint`、注意は琥珀色の箱（`.warn-box`・`.mp-warn`）。
  **…メニュー・共有シート・メンバーの成績は中身を `.sheet-body` で包む**と、スマホ幅（600px以下）で下から出るボトムシートになる
  （`#form-modal:has(.sheet-body)`。`:has` の無い端末は今までの中央のダイアログ。設定の編集など他の form-modal には効かない）
- `showView`: **未ログインで history・mystats を開くとログイン画面（`view-login`）**、ログイン中にログイン画面を開くとマイページ。
  ヘッダーの`goMyPage()`・試合の画面の下の「マイページ（過去の試合・自分の成績） ›」も同じ振り分け。`#mypage`・`#login` で開いたときはログイン状態が分かってから振り分ける（`pendingMyPage`）
- ログイン画面から押したログインは、戻ったらマイページを開く（`mahjong-after-login`。リダイレクトは押したときのURL＝/ に戻るため）
- **ログインは端末に保つ**（`setPersistence(LOCAL)`＝ブラウザを閉じてもログアウトしない）。iPhoneのSafariは7日間開かないとサイトのデータを消すことがあり、そのときだけログインし直し（記録はアカウント側に残る）
- **ログアウトすると、この端末の控え（過去の試合・自分の選択・仲間ページの一覧）を消す**。記録はアカウントに残っている。残すと、同じ端末で次に別の人がログインしたときにその人のアカウントへ混ざるため
- 未ログインでも、この端末で開いた試合は裏で控えておく（`saveToLocalHistory`）。**画面には出さず、ログインしたときにアカウントへ引き継ぐため**。
  トップの「最近の試合」は、未ログインなら「この端末の過去の試合 N件 → ログインするとずっと残せます」の1行だけ
- 旧「引き継ぎURL」（`#my=`）は、ログインしてアカウントへ移す入り口としてだけ残す（`openMyListFromUrl`→`importPendingMyList`）
- 「あなたはどれ？」はログイン中だけ出す（その場限りで遊ぶ人には聞かない）。選択肢は名前・「自分は出ていない」・「あとで」

## 共有シートと見るだけのリンク（2026-09-26〜）
- **試合のURL（`#試合ID`）は、開いた人が点数を入力・修正できる**＝同じ卓の仲間に送るもの。**SNSに載せる・観戦の人や来られなかった人に送る**ときは、
  点数を変えられない**見るだけのリンク（`#v=見るだけID`・12文字）**を使う
- **共有シート**（`openShareSheet(id?)`。試合の画面・結果詳細のタイトル横「共有」、過去の試合の「…」→共有）:
  ①一番上＝「一緒に打つ人に送る」（入力できるURL・コピー・LINEで送る）②「見るだけのリンク」（まだ無ければ「作る」ボタン＝`makeViewLink`）
  ③結果を画像にする。**その場限りで使う人が迷わないよう、①をいちばん目立たせ、作成直後の画面（view-share）は今までどおり①だけ**
- **仕組み（試合IDを漏らさない）**: `sessionViews/{試合ID}` = 見るだけID（試合IDを知っている人だけが引ける）／
  `views/{見るだけID}` = { s: 試合の写し（誰でも読める。name・createdAt・settings・rounds＋指紋h・操作数lv・更新日時at。**試合IDとlogは載せない**）,
  p: 「試合ID_n」, n: 書くたびに変える12文字 }（p・n は読めない）。ルールが p から試合IDを取り出して試合があるか確かめ、n を毎回変えさせるので、
  **リンクしか知らない人は写しを書き換えられない**（database.rules.json。確認は tools/view_rules_test.py）
- **写しの更新**: 試合を開いている端末（リスナーが値を受け取るたび）と、点数を送った端末（送信キューで確定したop）が`noteGameChanged(sid)`→
  1.2〜2.7秒待って`syncViewNow`。写しの元は**サーバーの値**（`serverGame`か sessions を読み直す。未送信の入力は載せない）。
  **中身の指紋 h が同じなら書かない／サーバーの写しの lv の方が大きい（こちらが古い）なら書かない**＝全員が同時に書いても内容は1つに収束する
- ルールを貼る前（sessionViews が読めない）は `viewFeature=false` で、共有シートに見るだけのリンクの欄を出さない
- **見るだけの画面（`view-watch`）**: 結果詳細と同じ部品（`renderDetailBody`を`detailRO=true`で描く）で、試合行のタップ・「仲間ページに追加」・設定の編集を出さない。
  `#v=`で開いたら `views/{ID}/s` を聞き続けて自動で更新（「最終更新 hh:mm」）。**その端末の過去の試合には入れない・「あなたはどれ？」も出さない**。
  最後に「まじゃすこを使ってみる」の案内（LPへ）。見つからないリンクは「見つかりませんでした」
- 仲間ページを見ているだけの人（メンバーでない人）が試合を押すと「結果を見る」＝`openWatchGame(circleSess[sid], 'circle')`（入力の画面は出さない）。
  メンバーは今までどおり「試合を開く（入力・修正）」
- 起動時のURL判定: #v= は #c= の後・セッションIDの前（「v=」＋12文字は8文字以上の条件も満たすため）

## 仲間ページ（circles・2026-09-25〜）
- **目的**: その場限りの試合とは別に、身内・大人数・大会の試合を仲間ごとにまとめ、自分が入っていない試合も含めた通算順位・対戦成績を全員で見る。
  1人が複数の仲間ページを持てる。1つの試合を複数の仲間ページに入れてもよい
- **立場**: 見る人（閲覧リンク `#c=ID`・ログイン不要・LINEの中でもOK）／編集メンバー（ログイン＋招待リンクか参加コードで参加）／
  管理者（作成者が任命）／作成者。**点数入力（sessions）は今までどおり誰でもログインなし**。ログインが要るのは仲間ページへの追加から先だけ
- データ（ルールは docs/site-spec.md）:
  - `circles/{cid}` = { id, name, createdAt, owner, roster: {mid: {name, at, uid?}}, games: {sid: {at, name, addedBy, addedByName, addedAt, seats: {席: {mid, by, byName, at, self}}}}, log }
  - `circleMembers/{cid}/{uid}` = { role: owner|admin|editor, name, joinedAt, code?, mid? }（メンバーだけが読める）
  - `circleSecrets/{cid}` = { code }（メンバーだけが読める）／`circleInvites/{code}` = cid（コードを知っていれば引ける）
  - `users/{uid}/circles/{cid}` = { at, name }（自分が参加している仲間ページ）。見ただけの仲間ページは localStorage `mahjong-circles-v1`
- **「本人」と「他人がまとめただけ」を区別する**:
  - 名簿の人に`uid`がある＝その人がログインして「これは私」と登録した（**✓本人**）。無い＝名前で結びつけただけ
  - 席の結びつけ`seats`は、結びつけた人（by・byName）と日時を必ず持つ。`self:true`＝本人が自分の席を結びつけた
  - **self の席は、本人・作成者・管理者だけが変えられる**。本人が登録した名簿の人の名前変更・名簿からの削除も同じ（`canChangeSeat`・`canEditRosterMember`。ルールでも同じ制限）
  - 本人登録すると、ほかの人がその人に結びつけていた席を「本人として確認」できる（`confirmMySeats`）
- **試合の追加**: 仲間ページの「試合を追加」（この端末の試合から選ぶ＋LINEのトーク履歴などを貼り付けて`extractSessionIds`で取り込む）、
  または試合の画面・結果詳細の「👥 仲間ページに追加」（名簿と名前がよく重なる仲間ページが上に並ぶ）。
  名前は`autoBindNames`（personKeyが同じ＝同じ人・仮名A・B…は結びつけない）。まとめて取り込むときは名簿にない名前を新しい人として足す。
  試合の画面から1試合だけ足したときは、未結びつけがあれば結びつけの画面をすぐ開く
- **画面はタブで切り替える**（2026-09-26。`circleTab`）: **順位**（表彰台＋リスト。指標＝通算スコア/平均着順/トップ率/ラス回避/1試合平均。
  スコア以外は**いちばん多く打った人の3分の1以上の試合数の人だけで順位を決め**、少ない人は「参考」として下に分ける＝規定打席と同じ考え方）／
  **成績表**（`circleGridHtml`。列＝メンバー（通算スコア順）・行＝試合数/ゲーム数/平均着順/1..n着/トップ率/連対率/ラス回避/通算スコア/1試合平均/最高・最低点数。
  いちばん良い値はオレンジ文字＝ゲームの個人成績と同じ）／**対戦**（だれから見る？＋相手別の表＋総当たり表`circleMatrixHtml`）／**試合**／**メンバー**（名簿＋各人の順位・成績の要約・変更の履歴）。
  四麻と三麻があるときは順位・成績表・対戦の上に切り替え。画像は「通算順位」＋「成績表」
- **メンバーのページ**（`openCircleMember`）: アイコン・✓本人・「通算○位 / n人」→成績のタイル・着順の分布・最近の着順・スコアの推移（マイページの「個人」と同じ部品）→
  相手ごとの成績→結びついている試合→（メンバーなら）これは私・名前の変更・同じ人としてまとめる
- **表記ゆれ**（「たろう」と「太郎」）は、名簿の人のページの「同じ人としてまとめる」で直す（席を付け替えて名簿から消す）。本人登録された人は消える側にできない
- **変更の履歴**（`circles/{cid}/log`・追記のみ）: 誰が・いつ・何をしたか。結びつけ・試合の追加・名前の変更は「元に戻す」ができる
  （戻すと、元に戻した人の結びつけとして書き直す＝ルール上、他人の名前で書けないため）
- **招待と参加**（`view-join`・`#join=コード`）: ①LINEの中→「Safari・Chromeで開いて参加する」＋参加コード表示 ②未ログイン→「Googleでログインして参加」
  （参加コードを localStorage `mahjong-pending-join` に覚えてからリダイレクト。戻ったら`resumePendingJoin`が自動で参加を続ける）
  ③ログイン済み→自動で参加（ログイン状態が後から分かる場合も onAuthStateChanged で続ける）→仲間ページが開き「名簿のどの人ですか？」を聞く。
  招待の画面には必ず「参加せずに結果だけ見る」を置く
- 参加コードは8文字（大文字・紛らわしい文字なし）。作成者が作り直すと古い招待リンクは使えなくなる（参加済みの人はそのまま）
- **集計**: `circleStats(circle, sessions, mode)`＝名簿の人に結びつけた席だけを数える（ゲスト・未結びつけ・名簿から消えた人の席は数えない）。三麻と四麻は別。
  対戦成績は`h2hAdd`をそのまま使う（keyOf＝席→名簿の人）
- 作成の順番に注意（ルールが前の書き込みを見るため）: 仲間ページ本体 → 作成者のメンバー登録 → 参加コード → 名簿の自分（uid付き）。
  削除は逆に、メンバー → 参加コード → 本体
- **LOGIN_ENABLED が false の間は、仲間ページの作成・参加・追加の入り口を出さない**（閲覧リンクで見ることはできる）

## その他の機能
- **結果の画像出力**: **画面の見え方（横スクロール中かどうか等）に関係なく、Canvasに描き直した一枚絵PNG**を生成。
  DOMスクショ方式ではなくCanvas直描画（iOS SafariのforeignObject非互換を避ける＋常に崩れない絵にするため）。
  出力はスマホ=共有シート（navigator.share・キャンセル時は何もしない）／非対応=PNGダウンロード（`shareCanvasImage`）。track('image_export')
  - **対象6セクション**（`IMG_SECTIONS`）: 総合順位`score`／チーム順位`team`／試合結果`rounds`／個人成績`stats`／
    スコア推移`chart`／個人スコア推移`ichart`。team・ichartはチーム戦のみ。
    各カードに**「プレビュー」ボタン**（ゲーム画面・結果詳細の両方。個人戦・チーム戦、三麻・四麻すべてで出る）
  - **構造**: `imgSectionBodyCanvas(g,kind)`が**本体だけのCanvas**を返し、
    単体出力`buildResultCanvas`＝ブランド帯＋本体＋フッター、まとめ出力`buildComposedCanvas`＝全体ヘッダー＋［見出し＋本体］×n＋フッター、
    と共用する（見た目の一貫性と実装の一元化）
  - 本体の描き方は3種: 表（`imgTableBodyCanvas`＝画面と同じHTMLを非表示DOMに展開してセル情報を抽出）／
    順位リスト（`imgRankingBodyCanvas`＝`.score-card`から抽出。**順位は絵文字でなく金銀銅の丸バッジ**で描く＝環境差なく同じ絵になる。
    **名前は2行まで折り返し**、折り返した行数ぶんその行だけ高さを増やす＝40文字のチーム名も切らずに読める）／
    グラフ（`imgChartBodyCanvas`＝`chartData`の計算結果からCanvasに直接線を引く）
  - **画像の折り返しは書記素クラスタ単位**（`imgGraphemes`＝Intl.Segmenter、非対応環境はコードポイント）。
    コードポイントで切るとタイ語の母音記号・アラビア語の連結・ZWJ絵文字（👨‍👩‍👧）が壊れて重なって描かれる。
    入りきらない場合は最終行の末尾を「…」にする（`imgWrap`が付けるので呼び出し側で足さないこと）
  - **区切り線は画面と同じ3種を描く**: チームの境目（`c.tb`）／先頭列の右（`c.left`）／チーム名行の下（`c.teamRow`）。
    色は見出し行=rgba(255,255,255,.85)・本文=#dfe8e6（CSSのborderはCanvasに反映されないので自前で引く）
  - 試合結果は画面と同じ「スコア＋薄い点数＋チップ差」の縦積み＋合計行
    （セル内の2行目以降は`subs`として9pxで描く。列幅・行高もsubsを含めて計算する）。
    チーム順位はメンバー名をサブ行で表示
- **画像プレビュー**（`openImagePreview`）: カードの「プレビュー」ボタン（`showResultImage`）とまとめ作成は、
  **まず全画面プレビューを開く**（保存せずその場で結果を確認するため）。書き出したCanvasをそのまま表示するので保存物と同一の絵。
  中身: タップで幅フィット⇄等倍のズーム切替、「PDF・印刷」（`printPreviewImage`＝`@media print`で画像だけを紙面に出す。
  スマホの印刷画面から「PDFとして保存」できる）、「保存・共有」（`shareCanvasImage`）。
  画像はblob URLで渡し、閉じるときにrevoke（`img.src=''`はページURLを読みに行くので`removeAttribute`で外す）
- **まとめ画像**（`openComposeModal`→`runCompose`→`buildComposedCanvas`）: 入れる項目をチェックで選び、
  **縦長1枚（幅1080px）**に連結する。ゲーム画面と結果詳細の末尾に「結果をまとめてプレビュー」ボタン。
  各セクションは内側幅に収まるよう縮小して白い角丸カードに載せ、上に見出しを付ける。
  **見出しと白カードは重ねない**: `textBaseline='middle'`なので見出し（15px）はy+12を中心に約y+3〜y+21を占める。
  カードは影（blur6・offsetY2）ぶんも上にはみ出すので、`LABEL_H`はカード上端(y+LABEL_H−CARD_PAD)が
  文字の下端より十分下に来る値にする（**36**。以前は30で約4px重なっていた）
  **iOS Safariのcanvas上限（1辺8000px・総ピクセル数16.7M）を超えるとtoBlobが黙ってnullを返し、保存もプレビューも失敗する**。
  上限対策は`imgCanvas`に集約: **scを省略すると`imgFitScale(w,h)`が上限に収まる倍率を自動で選ぶ**ので、
  表・グラフ・単体出力・まとめ出力のすべてに自動で効く。実倍率は`c._sc`に持たせ、CSSサイズは`imgCssSize(c)`で逆算する
  （`body.width / IMG_SCALE`と決め打ちしてはいけない）。
  倍率の下限は`IMG_MIN_SCALE`=0.7（本文13pxが約9px＝読める限界）。
  **収まらないときの段階的な対処**: ①列が多いほど1列の上限幅を下げる（`COL_MAX`＝27列以上84px／15列以上104px／既定132px。
  40人でも幅8000pxに達しない）②それでも収まらない試合結果は**薄い点数・チップ差を省いた縮約版**で作り直す
  （`roundRowHtml(..., compact)`。行高が約6割になり収容できる試合数が約1.7倍。帯のラベルに「（スコアのみ）」と付く）
  ③それでもダメなら壊れた画像を作らず`IMG_TOO_LARGE`を返し、呼び出し側がCSVを案内する。
  **判定は四捨五入後の実ピクセルで行う**（ぴったり上限になる倍率は丸め誤差で1px超えるため、面積由来の倍率は0.999倍しておく）。
  実測: 16人4チーム100試合＝全項目可（試合結果の文字17px）／40人4チーム100試合＝全項目可（同12px）／
  40人10チーム300試合＝全項目可（同9px）／500試合＝試合結果の単体のみ案内、他は可
- **CSVエクスポート**（「CSV」ボタン→`openCsvModal`→`runCsvExport`→`exportCsv(g, opts)`/`buildCsv(g, opts)`）:
  導線は**画面末尾の「CSVで書き出す」ボタン1か所だけ**（ゲーム画面・結果詳細とも「結果をまとめてプレビュー」の直下）。
  カード上にボタンが並びすぎて見にくくなるため、2026-08-06に試合結果カードの右上から移した。
  見た目は`.auto-btn.sub`（白地＋緑枠／三麻は青。オレンジはCTA限定なので主役の「まとめてプレビュー」とは配色を分ける）。
  表示条件はcompose-btnと同じ＝試合が1件以上（カードの開閉には連動しない）。
  押すと**項目選択モーダル**（`csv-modal`）が開く。選べるのは
  スコア／点数（素点）／チップ／焼き鳥／チョンボ／日時／合計行／グループ情報の8つ。
  **既定は最低限＝スコアと合計行だけON**（`CSV_DEFAULT`）。選択は`mahjong-csv-opts-v1`に記憶し次回も同じ状態で開く。
  チップ/焼き鳥/チョンボは**設定有効または記録ありのときだけ**選択肢に出す（旧データ互換）。
  そのとき出さなかった項目のON/OFFは保存時に書き換えない（`csvItemKeys`にある分だけ更新）。
  値の列が0個なら書き出さず注意を出す。試合番号は常に入る。
  **1人あたりの値列が1つだけのときは見出しを名前だけにする**（「山田 スコア」ではなく「山田」）。
  メタ「ゲーム情報」（ゲーム名/作成日/試合形式/チーム編成※チーム戦のみ）を選ぶと先頭に4行＋空行が付く。
  **UTF-8 BOM付き**（Excel日本語環境の文字化け対策。ソース中のBOMは`﻿`のエスケープ表記で書く=生文字は編集事故のもと）。
  列順は試合結果と同じ（チームA→B…）。不参加は空欄・マークは○。
  ファイル名「まじゃすこ_ゲーム名_YYYYMMDD.csv」（禁止文字は_に置換）。track('csv_export')
- **過去の試合への導線は2本**: ①ホームの「最近の試合」欄の「過去の試合をすべて見る ›」（履歴が**1件でもあれば必ず**出す）
  ②ゲーム画面末尾の「過去の試合・引き継ぎURL ›」（leaveGame→showView('history')）。
  引き継ぎURLカードの発行場所がこの画面なので、導線を絞りすぎない
- **結果詳細からの編集も必ずFirebase（sessions/{id}）へ書く**（2026-08-08。名前編集`saveDetailEdit`・
  試合修正`editDetailRound`・復元`applyRestore`のdetail側）。かつて端末内の控えだけを更新していたため、
  「再開」「最近の試合」「共有URL」などサーバーから取り直す入り口を通ると編集前に巻き戻る不整合があった。
  **データの正は常にsessions側**。書き方は「最新を取得→編集を載せて書く」（fetch-rebase-write）:
  名前編集は最新settingsにplayerNamesだけ差し替えて部分update（メンバー数が変わっていたら中断して最新化）、
  試合修正はシートを開く前に最新roundsへ更新してから編集（古い控えのまま全試合を書き戻すと他の人の新しい試合が消える）。
  サーバーへ書けたときだけ端末の控えも更新（片方だけ更新すると巻き戻り事故が再発する）。
  名前編集は登録名（savePlayerNames）とmylistsのname にも反映
- 過去の試合: カードは**ゲーム名・日付・総試合数・四麻/三麻・個人戦/チーム戦・プレイヤー名のみ**（結果のプレビューは載せない）。
  文字は13pxで統一、グループ名はellipsis・.history-infoにmin-width:0（枠はみ出し防止）。絞り込み=キーワード+四麻/三麻。
  カード右上に「再開」と**「削除」**（`confirmDeleteHistory`→確認モーダル必須→`deleteHistoryItem`）。
  削除で消えるのは**この端末の履歴＋引き継ぎURLの一覧**（mylistsから外さないと次の同期で復活する）＋分類タグのみ。
  **sessions/のデータ本体には触れない**＝仲間の画面には影響せず、共有URLを開けば自分でもまた表示できる（確認文にも明記）。
  確認モーダルのOKボタンは使い回しなので文言「削除する」を毎回セットし直す。
  **削除の同期設計（2026-08-08）**: 削除の影響範囲は
  「この端末の履歴・分類タグ」「引き継ぎURLのリスト（削除印）」「同じ引き継ぎURLを使う自分の別端末（伝播で消える）」のみ。
  **sessions/には触れない＝仲間の端末には一切影響しない**（グループ本体は残り、共有URLでいつでも再表示できる）。
  仕組みは**削除印方式**: `deleteHistoryItem`は`mylists/{id}/groups/{gid}`をremoveせず
  **`{deleted:true, at}`を書く**（数十バイト・コスト無視できる）。
  removeだと別端末が「この端末にだけある新規」と誤解してリストへ書き戻し、削除が復活してしまう。
  `mergeMyListData`は印を見て ①取り込まない ②ローカルの控え・タグも消す（**伝播**）③書き戻さない（myListSynced.add）。
  伝播側にはローカル墓標を付けない＝**リストの印が唯一の権威**（誰かが開き直して印が消えたら次のマージで自然復帰）。
  印の解除は「自分から開き直す」操作で: `joinSession`が`unmarkDeleted`＋`myListSynced.delete`→
  `syncGroupToMyList`のupdateに常に`deleted: null`が入っていて印ごと上書きする。
  **ルール未適用環境へのフォールバック**: 印の書き込みが拒否されたら従来のremoveに退化（壊れない。
  rules.jsonに`deleted`キーを追加済み→コンソール手動反映が必要）。
  ローカル墓標（localStorage `mahjong-deleted-v1`＝gid→削除日時、200件超で古い順に破棄）は
  オフラインで印を書けなかった場合の保険としてtoFetch除外に使う。
  **削除直後の「元に戻す」トースト**（`UNDO_TOAST_DELETE_MS`**4秒**・showUndoToastに関数を渡す形）＝
  控えコピーの復帰＋タグ復帰＋墓標解除＋syncで印も解除（`restoreDeletedHistory`）。
  **対局中グループの削除時は`stopListening()`**（popstateで戻るとリスナーが生きたままになり、
  次のvalueイベントの`saveToLocalHistory`が削除を数秒で巻き戻すため）
- 履歴ログ: フィルタタブ（すべて/入力/修正/削除。タグと同配色=緑/オレンジ/赤）。点数は「名前 点数」ペア表示
- **変更をもとに戻す（端末内バックアップ）**: 削除・修正・設定変更・名前編集の**直前の状態**（name/settings/rounds）を
  localStorage `mahjong-backup-v1` に控える（`takeBackup(g, label, expect)`）。
  **Firebaseには一切書かない**＝保存容量・通信コスト・セキュリティルールは一切変わらない（rules更新も不要）。
  保持は**1グループ5世代・全体900KBまで**（超えたら古いものから捨てる。QuotaExceededでも丸ごと失敗しないよう
  減らして再試行する`writeBackups`）。
  導線は2つ: ①操作直後のトーストの「元に戻す」（`showUndoToast(msg, key, ms)`。`.toast.act`でpointer-eventsを有効化。
  **表示時間は取り返しのつかなさで変える**: 削除・修正・名前更新=`UNDO_TOAST_MS`**8秒**／
  設定の更新=`UNDO_TOAST_SHORT_MS`**3秒**（何度でもやり直せる操作なので短くしてよい。通常のトーストは2.5秒））
  ②履歴モーダル上部の一覧（`backupSectionHtml`。タブより上に置く＝フィルタの対象ではないため）。
  戻すと`sessions/{id}`にname/settings/roundsを書き戻す＝**全員の画面に反映される**。復元自体も直前を控えるのでやり直せる。
  **他の人の入力を巻き戻さないためのガード**: 控えた時点で想定した試合数（`expect`）と現在の試合数が食い違うときは
  即時復元せず確認ダイアログに回す（`restoreBackupById(key, false)` → `confirmRestore`）。履歴からの復元は常に確認あり。
  過去の試合（結果詳細）から戻す場合はlocalStorageの控えを書き換える（行き先の判定は`restoreTargetOf`）
- 設定変更モーダル: 試合形式は変更不可（文言も「試合形式」で統一。対局形式/対戦形式とは書かない）。ルール変更は全再計算（⚠表示）。ヘッダー右に小「キャンセル/保存」（closeFormModalがクリア）。
  メンバー削除は対局済み/最低N人なら不可。チーム戦OFFでもteams保持

## テスト
- **実行環境**: smoke / sync / account の3本はPlaywright（Python版＋Chromium）が要る。circle_rules / view_rules は標準ライブラリだけで動く。
  CIがpushごとに回すのはsmoke_testだけ（sync・accountは手元でしか回らない）。導入手順は docs/setup-new-pc.md
- /tests.html = index.htmlをiframeで読み実物関数を49ケース検証（うち16件が送信キューの applyOp、対戦成績の h2hAdd・personKey、仲間ページの circleStats など）。**計算ロジック・保存処理の変更時はALL PASS確認必須**
- tools/smoke_test.py = 主要動線のE2E（デプロイ時にCIが自動実行。手元のWindowsは `python tools/smoke_test.py`、
  リモート環境のLinuxは PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers python3 tools/smoke_test.py）。
  Firebaseはスタブ（transaction・.info/connected も実装済み。スタブは shoot_app_screens.py / record_app_demo.py にもあり、3つそろえること）
- tools/sync_test.py = **本番のFirebaseで**2台の端末を再現して、圏外復帰で他人の試合が消えない／圏外で入力→再読み込みで消えない／
  二重登録しない／設定変更の履歴が残る／設定の同時編集で上書きしない、を確認。検証用ゲームは最後に自動で削除する。
  **保存処理を触ったら必ず通す**（本番のセキュリティルールで弾かれないことはここでしか確認できない）
- tools/account_test.py = ログイン・アカウント同期・仲間ページ（作成・追加・タブ・メンバーの成績・招待/参加・本人の区別・管理者・閲覧・LINE・ログインをまたぐ参加）・
  マイページ（個人｜仲間）・共有シート・見るだけのリンク（写しに試合IDが入らない・点数を入れると写しも更新・入力の入口が無い）・共有URLを**スタブのFirebase**で確認（本番DB・Googleに触らない）。
  auth をスタブにして「ログインした」状態を作る。**ログイン・引き継ぎ・まとめ・共有URLを触ったら通す**。
  本物のルールでの動きは分からないので、database.rules.jsonを変えたら本番で手で確かめる
- tools/circle_rules_test.py = **本番のデータベース**で仲間ページのセキュリティルールを確認（匿名ログインで使い捨てのアカウントを3つ作り、
  「本人の席は他人が変えられない」「部外者は書けない」「参加コードが要る」などの許可／拒否を39項目。最後にデータとアカウントを消す）。
  **database.rules.json の仲間ページの部分を変えたら、コンソールに貼ってからこれを通す**（Authenticationで「匿名」を一時的に有効にする）
- tools/view_rules_test.py = **本番のデータベース**で見るだけのリンクのルールを確認（ログイン不要。「写しは誰でも読めるが試合IDの入った p は読めない」
  「リンクしか知らない人は書き換えられない」「別の試合のIDで乗っ取れない」など20項目。検証用の試合と写しは最後に消す）。
  **database.rules.json の views / sessionViews を変えたら、コンソールに貼ってからこれを通す**

## 落とし穴
- **会社PCから本番（majasco.jp）をPlaywright・ブラウザペインで開くと、Firebaseにつながらない（dbConnected=false）**ことがある（2026-09-26）。
  社内のDNS/プロキシでFirebaseの宛先がローカルのアドレスになり、ChromeのLocal Network Accessが遮断する（`ERR_BLOCKED_BY_LOCAL_NETWORK_ACCESS_CHECKS`）。
  アプリの不具合ではない。Playwrightは `launch(args=["--disable-features=LocalNetworkAccessChecks"])` で起動して確かめる（localhostから開くテストは影響なし）
- **画面への導線を条件付きにすると、その画面の機能ごと見つからなくなる**（2026-08-07に発生。
  「過去の試合」への唯一の入り口が履歴3件以上でしか出ず、引き継ぎURLの発行場所が見つからないと報告された。
  新機能を置いた画面の入り口は「常に出るか」を必ず確認する）
- **起動時のhistory.replaceStateで第3引数にlocation.pathnameだけを渡すと、共有URLの#セッションIDが消えて自動参加が壊れる**
  （2026-07-18に発生。「共有URLを開いてもLPしか出ない」の原因。必ず `location.pathname + location.hash` にする）
- iOSで transform＋fixed＋input はタップ不良を起こす（ボトムシート廃止の理由）。モーダルはtransformなしで
- requestAnimationFrameは非表示タブで発火しない → UI更新に使わない（カルーセルのドットで事故った）
- .toggle-btnはbackgroundにtransitionがあり、切替直後のgetComputedStyleは中間色を返す（検証時の偽陽性）
- ブラウザ検証: プレビューのモバイルエミュレーションではfixed要素の幅がペイン実幅になる（scrollWidth偽陽性。実スクロールはwindow.scrollXで判定）
- チーム編成エディタ再初期化前に必ず captureTeamNames()（入力中のチーム名が消える）
- 確認モーダルのOKボタン（#confirm-modal-ok）は使い回し。文言も用途ごとに毎回textContentでセットし直す
  （削除=「削除する」／復元=「戻す」。片方だけ書き換えると次の削除で「戻す」と表示される）
- `.toast`はleft:50%だけだと使える幅が画面の半分（187px）に制限され、長い文言が折り返して2行になる
  → `width: max-content` ＋ `max-width: calc(100vw - 24px)` で内容なりの幅にする
- グリッド／flexアイテムはmin-width:autoで内容に引きずられて広がる → min-width:0 を忘れない
  （2026-08: 区切りのない長い半角英数字のチーム名で、順位カードのスコア値が画面外に押し出された）
- Canvasで長文を手で折り返すときは書記素クラスタ単位で切る（コードポイント単位だと結合文字が壊れる）
- CSS zoomを小さくすると、ブラウザの最小フォントサイズ設定で文字だけ縮まなくなり、table-layout:fixed＋ellipsisのセルは文字が消える
  → 縮小しうる表はtable-layout:autoにし、消えて困るラベルにellipsisを使わない（画像出力の`forImage=true`がこれ）
- **表の縮小にCSS zoomを使ってはいけない**（2026-08-06に実機で発覚）。zoomはレイアウトごと縮むため、
  モバイルの最小フォントサイズ制限で「セルは縮むのに文字は縮みきらない」＝文字がセルからはみ出す。
  縮小は必ず`transform:scale`（見た目だけ縮み、レイアウトは不変なので構造的にはみ出さない）
- **iOS Safariの自動文字拡大（text autosizing）**は、横幅が画面より広いブロック＝横スクロールする表があると発動し、
  文字だけ勝手に大きくなってセルから溢れる → `html { -webkit-text-size-adjust: 100% }` で抑止済み（消さないこと）
- `.rounds-table th.sticky-col`（0,2,1）はクラス2つの上書き（0,2,0）に勝つ → 上書きは同じくth/td付きで書く（lp-specのbm-noteと同じ詳細度事故）
- **「端末内の控えだけを更新する編集」は必ず巻き戻る**: ローカル履歴はjoinSession（再開・最近の試合・共有URL）の
  たびにサーバーの内容で丸ごと上書きされるため、sessionsに書かない編集はいつか消える（2026-08に名前編集で発生）。
  編集は常にsessions側へ書き、ローカルはその写しとして更新する
- **iOSのホーム画面アプリ（display:standalone）はブラウザと別の保存領域**（localStorage・IndexedDBとも非共有）。
  さらにリンクは常にブラウザ側で開く（アプリはリンクを受け取れない）。
  「ホーム画面に追加すれば履歴が守られる」と案内するだけだと**アプリ側は履歴が空**で混乱させる（2026-08に実際に起きた）
  → 橋渡しは引き継ぎURL（貼り付け読み込み＋起動時のrefreshMyList）で行う
- **sticky列（z-index:1）はborder-collapseの罫線より後に自分の背景を描く**ので、その列だけ線が消える／細く見える。
  右端だけでなく**下端も同じ**（2026-08「チーム」セルの下の線が隣と違う太さに見えた）→ borderを0にしてbox-shadow insetで引き直す
- **Canvasの見出しと本体を重ねない**: `textBaseline='middle'`のfillTextは指定yが文字の“中心”。
  行送りを文字サイズと同じにすると次の要素に食い込む（2026-08 まとめ画像の「総合順位」等が白カードに約4px重なった）。
  影付きのカードは`blur+offsetY`ぶん上にも広がることも足して余白を決める
- **`-webkit-line-clamp`をthに直接掛けてはいけない**（`display:-webkit-box`にするとセルでなくなり表が崩れる）→ 内側のspanに掛ける
- paddingのあるスクロールコンテナ直下でposition:sticky; left:0を使うと、固定位置はpadding端ではなくコンテナ端＝スクロール開始時にpadding分ジャンプする
  → sticky列の横スクロールはpadding無しの内側ラッパー（overflow-x:auto）に担わせる
- 画像出力は非表示DOMにHTMLを展開して読み取る方式なので、**三麻は`.sanma`クラスをそのコンテナに付ける**
  （付け忘れると画面は青なのに画像の表ヘッダーだけ緑になる）
- セッションに新しいトップレベルキーを足すときはdatabase.rules.jsonの許可リストにも追加（$other:falseのため、忘れると保存が全部失敗する。→ docs/site-spec.md）
- HTML→画像はSVG foreignObject+canvas方式だとiOS Safariで壊れる/汚染される → 表の画像化はCanvasに自前描画する（buildResultCanvasの方式を踏襲）
- 検証でcanvasの絵をClaudeが目視したいときは、scratchpadのワンショット受信サーバー(save-server.js)へfetch POSTしてファイル化→Read（クリップボードはフォーカス制約で不可）
- 画像出力に絵文字（🥇🥈🥉等）をfillTextで描かない。環境によってモノクロ/豆腐になる → 色付きの図形で表現する
- **画像を作る表のセルに `medalSvg` やSVGの `<text>` を入れない**。imgTableHtmlCanvas はセルの textContent を読むので、
  メダルの数字が「11着」「1しろう」のように文字に混ざる（aria-hiddenでも防げない）。メダル・着順のタイル・アイコンは画面用（forImage=false）だけに出す
- **アイコンの差し色 `--i4: var(--i1)` は `.ico` の上で決める**。`:root` で決めるとその時点の #177083 に固定されて子に受け継がれ、
  三麻（.sanma の青）・削除（.danger の赤）で色が変わらなくなる
- スプライト `#icon-sprite` は `display:none` にしない（一部のブラウザで `<use>` の参照先が描かれない）。大きさ0で隠す
- `preserveAspectRatio="none"` のSVG（スコアの推移）の中に円を描くと横に伸びて楕円になる。点はSVGの外にHTMLで重ねる（`.spark-dot`）
- **テストで「ログイン」「招待から参加」の後に wait_for_function で待つときは `SAFE % "式"` で包む**（account_test.py）。
  どちらも location.reload() を挟むので、読み直し中（HTMLが途中まで・スクリプトがまだ動いていない瞬間）に評価すると
  ReferenceError・null の classList で落ちる。ページが重くなるほど起きやすい（2026-09-26にマイページの改修で頻発した）
- Canvasには端末ごとの上限がある（iOS Safariは総ピクセル数約16.7M）。縦に伸びる画像は必ず上限チェックを入れる。
  超えたときは黙ってtoBlobがnullを返し、保存が丸ごと失敗する
- **用語を変えたら doc 全体を grep して直す**（2026-09-17。2026-08-26の「収支」→「換算」で、新しい節は書き足したのに
  計算式・クイックスタート・結果表示・画像出力の記述が旧表記のまま残り、同じファイル内で矛盾していた）。
  節を足すだけでなく、旧語での grep が0件（＝残るのは「以前は〜」の経緯だけ）になるまで確認する
- **保存のたびに配列を丸ごと書くと、圏外から戻った端末が他の人の入力を消す**（2026-09に実際に点数が消えた）。
  Firebase(RTDB)の送信待ちはメモリにしか無く、再読み込みで消えるのも同時に起きていた。
  いまは送信キュー＋トランザクションで扱う（→「送信キュー」）。**配列の丸ごと書き込みを新しく足さないこと**
- **手元のテストが全部通っても、本番のセキュリティルールで弾かれることがある**（スタブにはルールが無い）。
  保存処理を変えたら tools/sync_test.py で本番DBに対して確かめる
